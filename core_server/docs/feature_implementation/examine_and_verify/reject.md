# examine_and_verify_reject
接收两个键值对，内容如下：
- 提案UID.
键名为`proposalUID`，键值为打回的提案的`UID`.
- 打回原因.
键名为`reason`，键值类型为字符串.
### 参数规范
`提案UID`必须在提案表中有对应的`提案`.
`打回原因`允许为空.
### 工作流程
1. 对`提案UID`对应的`提案`上读写锁，不对整张表上锁.
2. 返回`打回原因`.
3. 删除`提案UID`对应的`提案`.
### 错误处理
如果`提案UID`不存在对应的`提案`，则返回`unknown_proposal`.